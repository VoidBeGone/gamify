# LevelUp MCP server configuration

# Base URL of the LevelUp backend REST API (no trailing slash needed).
API_BASE_URL=http://localhost:3001/api/v1

# Shared secret — must match the API_KEY the backend expects.
# Sent as the `x-api-key` header on every request.
API_KEY=replace-with-your-backend-api-key
